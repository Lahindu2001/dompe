# 🏗️ System Architecture Overview

## Authentication Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                             │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│  (Client)    │
└──────┬───────┘
       │
       │ 1. Visit /login
       ↓
┌──────────────────┐
│  Login Page      │
│  (form input)    │
└──────┬───────────┘
       │
       │ 2. Submit email + password
       ↓
┌─────────────────────────────────────────────────────────────────┐
│              lib/api/auth.ts (API Service Layer)                │
│                                                                  │
│  - Validates input                                              │
│  - Calls Google Apps Script API                                 │
│  - Falls back to mock users (development)                       │
└──────┬──────────────────────────────────────────────────────────┘
       │
       │ 3. Receive user data
       ↓
┌─────────────────────────────────────────────────────────────────┐
│           contexts/AuthContext.tsx (State Manager)              │
│                                                                  │
│  login(user) {                                                  │
│    - saveAuthSession(user)  → lib/auth.ts                      │
│    - setUser(user)          → Update state                     │
│    - getRedirectPath(role)  → Determine destination            │
│    - router.push(path)      → Navigate                         │
│  }                                                              │
└──────┬──────────────────────────────────────────────────────────┘
       │
       │ 4. Redirect based on role
       ↓
┌──────────────┬─────────────────┬──────────────┐
│   Admin      │  Regular User   │  Shop Owner  │
│   (role:     │  (role:         │  (role:      │
│   'admin')   │  'user')        │  'shop_owner'│
└──────┬───────┴─────────┬───────┴──────┬───────┘
       │                 │               │
       ↓                 ↓               ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   /admin/    │  │  / (homepage)│  │   /owner/    │
│  dashboard   │  │   w/ session │  │  dashboard   │
└──────────────┘  └──────────────┘  └──────────────┘
```

## Session Management Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    SESSION LIFECYCLE                             │
└─────────────────────────────────────────────────────────────────┘

1. LOGIN SUCCESS
   ↓
   saveAuthSession(user, token)
   │
   ├─→ localStorage.setItem('auth_token', token)
   ├─→ localStorage.setItem('auth_user', JSON.stringify(user))
   └─→ localStorage.setItem('auth_expires', Date.now() + 7days)

2. PAGE LOAD / NAVIGATION
   ↓
   getAuthSession()
   │
   ├─→ Check if token exists
   ├─→ Check if session expired
   ├─→ Return user data OR null
   └─→ Update AuthContext state

3. LOGOUT
   ↓
   clearAuthSession()
   │
   ├─→ localStorage.removeItem('auth_token')
   ├─→ localStorage.removeItem('auth_user')
   ├─→ localStorage.removeItem('auth_expires')
   └─→ Redirect to /login
```

## Protected Route Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                  PROTECTED ROUTE LOGIC                           │
└─────────────────────────────────────────────────────────────────┘

User tries to access protected page
   ↓
<ProtectedRoute requiredRole="admin">
   ↓
   Check: isAuthenticated?
   │
   ├─→ NO ──→ Redirect to /login
   │
   └─→ YES
       │
       Check: user.role === requiredRole?
       │
       ├─→ NO ──→ Redirect based on actual role
       │         (admin → /admin/dashboard)
       │         (user → /)
       │
       └─→ YES ──→ Render protected content
```

## Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                      app/layout.tsx                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │            <AuthProvider>                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │           All Pages & Components                      │  │ │
│  │  │  ┌────────────────────────────────────────────────┐  │  │ │
│  │  │  │  <Header />                                     │  │  │ │
│  │  │  │    - useAuth()                                  │  │  │ │
│  │  │  │    - Show user menu if authenticated            │  │  │ │
│  │  │  └────────────────────────────────────────────────┘  │  │ │
│  │  │  ┌────────────────────────────────────────────────┐  │  │ │
│  │  │  │  Page Content                                   │  │  │ │
│  │  │  │    - Access user via useAuth()                  │  │  │ │
│  │  │  │    - Call logout() when needed                  │  │  │ │
│  │  │  └────────────────────────────────────────────────┘  │  │ │
│  │  │  ┌────────────────────────────────────────────────┐  │  │ │
│  │  │  │  <Footer />                                     │  │  │ │
│  │  │  └────────────────────────────────────────────────┘  │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYERS                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  Presentation   │  React Components (pages, UI)
│     Layer       │  - Login/Register Forms
└────────┬────────┘  - Admin Dashboard
         │           - User Profile
         │
         ↓ useAuth()
┌─────────────────┐
│   State Layer   │  contexts/AuthContext.tsx
│                 │  - Global auth state
└────────┬────────┘  - user, isAuthenticated, isLoading
         │           - login(), logout(), checkAuth()
         │
         ↓ calls
┌─────────────────┐
│  Business       │  lib/auth.ts
│  Logic Layer    │  - saveAuthSession()
└────────┬────────┘  - getAuthSession()
         │           - clearAuthSession()
         │           - Role checking functions
         │
         ↓ uses
┌─────────────────┐
│   Storage       │  Browser localStorage
│     Layer       │  - auth_token
└────────┬────────┘  - auth_user (JSON)
         │           - auth_expires (timestamp)
         │
         ↓ (for login/register)
┌─────────────────┐
│    API Layer    │  lib/api/auth.ts
│                 │  - loginUser()
└────────┬────────┘  - registerUser()
         │
         ↓ calls
┌─────────────────┐
│   External      │  Google Apps Script
│   Services      │  - User authentication
└─────────────────┘  - Data storage
```

## Role-Based Access Control

```
┌─────────────────────────────────────────────────────────────────┐
│                    ROLE PERMISSIONS                              │
└─────────────────────────────────────────────────────────────────┘

role: 'admin'
├─ Access: /admin/dashboard          ✅
├─ Access: /admin/*                  ✅
├─ Access: /profile                  ✅
├─ Access: / (all public pages)      ✅
└─ Can: Manage shops, users, reviews ✅

role: 'user'
├─ Access: /admin/*                  ❌ (redirects to /)
├─ Access: /profile                  ✅
├─ Access: / (all public pages)      ✅
└─ Can: Browse, review shops         ✅

role: 'shop_owner'
├─ Access: /owner/dashboard          ✅
├─ Access: /admin/*                  ❌ (redirects to /owner/dashboard)
├─ Access: /profile                  ✅
├─ Access: / (all public pages)      ✅
└─ Can: Manage own shop              ✅
```

## File Structure Map

```
dompelkbywarunaa/
│
├── lib/
│   ├── auth.ts                    🔑 Core authentication utilities
│   ├── api/
│   │   └── auth.ts               🌐 API service layer
│   ├── data.ts                   📊 Mock data (shops, reviews)
│   └── types.ts                  📝 TypeScript interfaces
│
├── contexts/
│   └── AuthContext.tsx           🎯 Global auth state management
│
├── components/
│   ├── auth/
│   │   └── ProtectedRoute.tsx    🛡️ Route protection HOC
│   ├── header.tsx                🔝 Navigation with auth
│   ├── footer.tsx
│   ├── shop-card.tsx
│   └── ui/                       🎨 Reusable UI components
│
├── app/
│   ├── layout.tsx                📦 Root layout with AuthProvider
│   ├── page.tsx                  🏠 Homepage (auth-aware)
│   │
│   ├── login/
│   │   └── page.tsx             🔐 Login page
│   │
│   ├── register/
│   │   └── page.tsx             📝 Registration page
│   │
│   ├── profile/
│   │   └── page.tsx             👤 User profile (protected)
│   │
│   ├── admin/
│   │   └── dashboard/
│   │       └── page.tsx         👑 Admin dashboard (admin only)
│   │
│   ├── shops/
│   │   └── page.tsx             🏪 Browse shops
│   │
│   └── categories/
│       └── page.tsx             📦 View categories
│
└── Documentation/
    ├── QUICK_START.md            🚀 Quick start guide
    ├── AUTH_DOCUMENTATION.md     📚 Detailed docs
    ├── IMPLEMENTATION_SUMMARY.md 📋 Implementation overview
    └── ARCHITECTURE.md           🏗️ This file
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                               │
└─────────────────────────────────────────────────────────────────┘

Level 1: Client-Side Protection
├─ Route Guards (ProtectedRoute component)
├─ Session Expiry Checks (7-day timeout)
├─ Role-based UI rendering
└─ Input validation on forms

Level 2: Session Management
├─ Token generation (unique per session)
├─ Secure storage (localStorage with expiry)
├─ Automatic session cleanup
└─ Session validation on every page load

Level 3: API Integration
├─ Google Apps Script authentication
├─ Fallback to mock users (development)
├─ Error handling and retry logic
└─ Network request validation

Level 4: Future Enhancements (Production)
├─ JWT tokens with signing
├─ HttpOnly cookies
├─ CSRF tokens
├─ Rate limiting
├─ Password hashing
└─ Email verification
```

## State Management Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                  AUTHCONTEXT STATE                               │
└─────────────────────────────────────────────────────────────────┘

AuthProvider
│
├─ State Variables:
│  ├─ user: AuthUser | null
│  ├─ isAuthenticated: boolean
│  └─ isLoading: boolean
│
├─ Functions:
│  ├─ login(userData)
│  │  └─ Updates user, saves session, redirects
│  │
│  ├─ logout()
│  │  └─ Clears user, removes session, redirects to login
│  │
│  └─ checkAuth()
│     └─ Validates existing session on mount
│
└─ Provides to all children:
   { user, isAuthenticated, isLoading, login, logout, checkAuth }

Usage in components:
const { user, isAuthenticated, logout } = useAuth();
```

## Request/Response Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    LOGIN REQUEST                                 │
└─────────────────────────────────────────────────────────────────┘

1. User submits login form
   │
   ↓
2. app/login/page.tsx calls login(user)
   │
   ↓
3. contexts/AuthContext.tsx.login()
   │
   ├─→ lib/auth.ts.saveAuthSession()
   │   │
   │   └─→ localStorage.setItem() × 3
   │
   ├─→ setUser(userData)
   │
   └─→ router.push(redirectPath)

Response Flow:
─────────────
localStorage updates
   ↓
AuthContext state updates
   ↓
All components re-render with new auth state
   ↓
Header shows user menu
   ↓
Protected routes now accessible
   ↓
User redirected to appropriate dashboard
```

---

**This architecture provides:**
- ✅ Separation of concerns
- ✅ Reusable components
- ✅ Type safety (TypeScript)
- ✅ Scalability
- ✅ Maintainability
- ✅ Security
- ✅ User experience
